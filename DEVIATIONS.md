# Deviations from the analysis plan

Every change made after tag `v2-frozen` to data, code, configuration or
analysis is recorded here.

| Date | Commit | Plan section | Change | Reason | Effect on results |
|---|---|---|---|---|---|
| 2026-09-17 | (next commit) | §6 environment | Added `tabulate==0.10.0` to the pinned environment; no other package changed (verified with `git diff env/requirements.lock`). | Needed by pandas to write Markdown tables for the article. | None: presentation only. |
| 2026-09-17 | (next commit) | §6 | Added `experiments/make_tables_figures.py` and `zsh/plotstyle.py` after the freeze. | Presentation code; reads saved results only. | None on any computed quantity. |
| 2026-09-17 | (next commit) | §2.2 collection mechanics | Block hashes and transaction counts are read from mempool.space `/api/v1/blocks/<height>` (15 headers per request) instead of `/block-height/<h>` plus `/block/<hash>` per block. The sampled heights, page offsets, page requests and parsing are unchanged. | At the observed ~1 request/s (blockstream.info rate-limited) the per-block requests would have taken ~15 h; the bulk endpoint cuts ~31,000 requests to ~6,700. Checked on block 863,600: identical hash and tx_count from both endpoints. | None on the collected data (same blocks, same pages). |
| 2026-09-17 | (next commit) | E8, §7 item 5 | `GMMU` now records a failure when none of the pre-declared `reg_covar` values (1e-4, 1e-3, 1e-2) fits, and E8 reports the Gaussian mixture as "failed to fit" instead of stopping. E8 was rerun. | On the full Elliptic training data (165 features) the diagonal mixture raised a covariance error at all three values, which ended the first E8 run before any result was written. | The Gaussian-mixture baseline is absent from the Elliptic results; nothing else changed. |
| 2026-09-17 | (next commit) | E5 (addition) | Added `experiments/e05b_weight_drift.py`, an exploratory comparison of the feature ranks and weights of the E5 refits with the development model (Kendall τ). | The TEST refit agreed poorly with the transferred partition; the ranks explain part of this. | New descriptive output only; reads saved models. |
| 2026-09-17 | (next commit) | E0, E7 | `e00c_fetch_d5.py` parses each txid once, and `e07_heuristic_validity.py` drops duplicate API records before merging. E0c was rerun from the local cache (no new requests). | Six txids had been drawn into both the equivalence sample and an E7 stratum, so their API records were stored twice and the merges counted them twice (2,006 instead of 2,000 equivalence rows; 5,003 instead of 5,000 rows per E7 stratum). | E0: `matched` is now 2,000; all agreement rates unchanged (1.0), the 99th-percentile relative errors change in the ninth decimal. E7: the estimates change only in the fourth decimal (precision of the count rule 0.0964 in both versions). |
| 2026-09-17 | (next commit) | E7 | For the prospective sample, E7 now also reports percentile intervals for the design-weighted estimates (1,000 resamples of blocks with replacement within each month), and the monthly rates in `future_by_month.csv` use the design weights. Added before any prospective data existed (collection still running). | The plan gave point estimates only; unweighted monthly rates over-represent small blocks. | New intervals; monthly rates now design-weighted. No other quantity changes. |
| 2026-09-17 | (next commit) | §6 | Added `experiments/make_manuscript_tables.py`, which writes compact, formatted versions of the result tables for the article. | Presentation code; reads saved results only. | None on any computed quantity. |
| 2026-09-17 | (next commit) | §2.2 collection mechanics | `zsh/collect.py` accepts two operational settings: `ZSH_PAGE_HOSTS` (fetch pages from a subset of the two configured hosts) and `ZSH_MIN_INTERVAL` (a longer minimum interval). From 15:55 IST the page requests ran on blockstream.info only, at ≥ 2.5 s per request. | From about 15:30 IST mempool.space refused every connection from this network (TLS handshake reset on all its published addresses; plain HTTP timed out), and blockstream.info answered bursts with HTTP 429. We did not try other addresses or hosts. | None on the collected data: both hosts are Esplora instances serving the same blocks; each cached response records its host. |
| 2026-09-17 | (next commit) | §2.2 | `e00d_collect_future.py`: (i) a month is kept only if at least 95% of its planned pages were obtained, and design weights divide by the pages obtained rather than planned (a missing page is treated as missing at random); (ii) `ZSH_FUTURE_CLOSE=1` builds the sample from the cache without new requests. Decision rule agreed with the authors at 16:20 IST, before any prospective data were analysed: if both hosts still refuse requests on 18 Sep 2026 at 09:00 IST, the sample is closed with rule (i), which keeps October 2024 to May 2025. | Both approved hosts blocked this network (see previous entry); the design cannot be completed without them. | If applied: the prospective sample covers 8 of the 23 planned months. The weights change only for months with a missing page (one page in November 2024 so far). |
