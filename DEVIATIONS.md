# Deviations from the analysis plan

Every change made after tag `v2-frozen` to data, code, configuration or
analysis is recorded here.

| Date | Commit | Plan section | Change | Reason | Effect on results |
|---|---|---|---|---|---|
| 2026-09-17 | `705505b` | §6 environment | Added `tabulate==0.10.0` to the pinned environment; no other package changed (verified with `git diff env/requirements.lock`). | Needed by pandas to write Markdown tables for the article. | None: presentation only. |
| 2026-09-17 | `705505b` | §6 | Added `experiments/make_tables_figures.py` and `zsh/plotstyle.py` after the freeze. | Presentation code; reads saved results only. | None on any computed quantity. |
| 2026-09-17 | `1baefa6` | §2.2 collection mechanics | Block hashes and transaction counts are read from mempool.space `/api/v1/blocks/<height>` (15 headers per request) instead of `/block-height/<h>` plus `/block/<hash>` per block. The sampled heights, page offsets, page requests and parsing are unchanged. | At the observed ~1 request/s (blockstream.info rate-limited) the per-block requests would have taken ~15 h; the bulk endpoint cuts ~31,000 requests to ~6,700. Checked on block 863,600: identical hash and tx_count from both endpoints. | None on the collected data (same blocks, same pages). |
| 2026-09-17 | `0977cc2` | E8, §7 item 5 | `GMMU` now records a failure when none of the pre-declared `reg_covar` values (1e-4, 1e-3, 1e-2) fits, and E8 reports the Gaussian mixture as "failed to fit" instead of stopping. E8 was rerun. | On the full Elliptic training data (165 features) the diagonal mixture raised a covariance error at all three values, which ended the first E8 run before any result was written. | The Gaussian-mixture baseline is absent from the Elliptic results; nothing else changed. |
| 2026-09-17 | `c298498` | E5 (addition) | Added `experiments/e05b_weight_drift.py`, an exploratory comparison of the feature ranks and weights of the E5 refits with the development model (Kendall τ). | The TEST refit agreed poorly with the transferred partition; the ranks explain part of this. | New descriptive output only; reads saved models. |
| 2026-09-17 | `f80a39c` | E0, E7 | `e00c_fetch_d5.py` parses each txid once, and `e07_heuristic_validity.py` drops duplicate API records before merging. E0c was rerun from the local cache (no new requests). | Six txids had been drawn into both the equivalence sample and an E7 stratum, so their API records were stored twice and the merges counted them twice (2,006 instead of 2,000 equivalence rows; 5,003 instead of 5,000 rows per E7 stratum). | E0: `matched` is now 2,000; all agreement rates unchanged (1.0), the 99th-percentile relative errors change in the ninth decimal. E7: the estimates change only in the fourth decimal (precision of the count rule 0.0964 in both versions). |
| 2026-09-17 | `4053cc4` | E7 | For the prospective sample, E7 now also reports percentile intervals for the design-weighted estimates (1,000 resamples of blocks with replacement within each month), and the monthly rates in `future_by_month.csv` use the design weights. Added before any prospective data existed (collection still running). | The plan gave point estimates only; unweighted monthly rates over-represent small blocks. | New intervals; monthly rates now design-weighted. No other quantity changes. |
| 2026-09-17 | `4053cc4` | §6 | Added `experiments/make_manuscript_tables.py`, which writes compact, formatted versions of the result tables for the article. | Presentation code; reads saved results only. | None on any computed quantity. |
| 2026-09-17 | `6a923f2` | §2.2 collection mechanics | `zsh/collect.py` accepts two operational settings: `ZSH_PAGE_HOSTS` (fetch pages from a subset of the two configured hosts) and `ZSH_MIN_INTERVAL` (a longer minimum interval). From 15:55 IST the page requests ran on blockstream.info only, at ≥ 2.5 s per request. | From about 15:30 IST mempool.space refused every connection from this network (TLS handshake reset on all its published addresses; plain HTTP timed out), and blockstream.info answered bursts with HTTP 429. We did not try other addresses or hosts. | None on the collected data: both hosts are Esplora instances serving the same blocks; each cached response records its host. |
| 2026-09-17 | `03bfc26` | §2.2 | `e00d_collect_future.py`: (i) a month is kept only if at least 95% of its planned pages were obtained, and design weights divide by the pages obtained rather than planned (a missing page is treated as missing at random); (ii) `ZSH_FUTURE_CLOSE=1` builds the sample from the cache without new requests. Decision rule agreed with the authors at 16:20 IST, before any prospective data were analysed: if both hosts still refuse requests on 18 Sep 2026 at 09:00 IST, the sample is closed with rule (i), which keeps October 2024 to May 2025. | Both approved hosts blocked this network (see previous entry); the design cannot be completed without them. | If applied: the prospective sample covers 8 of the 23 planned months. The weights change only for months with a missing page (one page in November 2024 so far). |
| 2026-09-17 | `c4ea9bd` | §2.2 collection mechanics | At 18:40 IST, on a different network connection, both hosts answered again; the collector was restarted with both hosts at ≥ 2 s per request per host (`ZSH_MIN_INTERVAL=2.0`). The machine was hibernated between 16:53 and 18:38 IST; requests interrupted by this were retried. | Resume the planned two-host collection at a rate that both hosts tolerate. | None on the collected data. |
| 2026-09-18 | `6c42ae3` | §3.3 (audit) | Added `results/E0/audit_extra.json`: the share of rows where `value_difference` equals the fee, the share where the value-concentration ratio is one, and the number of rows with zero output value, each for the whole sample and for the development period. | Two descriptive numbers quoted in Section 3.3 were computed ad hoc during the audit and were not in any result file; a check of the article's numbers against `results/` (`v2_work/verify_numbers.py`) found them missing. | None on any computed quantity. The article now states the scope of both numbers: 99.97% of all rows (99.96% of development rows) and 940 rows with zero output value in the whole sample. |
| 2026-09-18 | `6c42ae3` | §6 (presentation) | Added `experiments/verify_manuscript_numbers.py`: 177 checks that recompute quantities from `results/` and assert that the string the article uses for each occurs in the manuscript. | The reviewers found figures, tables and text that disagreed; this makes the agreement testable. | None on any computed quantity. All 177 checks pass for the submitted text. |
| 2026-09-18 | `d13776a` | §6 (presentation) | Added a table (Appendix A) with a one-line description of every profile and the development transaction closest to its centroid, built by `make_manuscript_tables.py` from `results/E1/profiles_dev.csv`. | Reviewer 3 asked for the distinguishing features of similar profiles with representative examples. | None on any computed quantity; the descriptions and transaction ids are taken from the E1 output. |
| 2026-09-18 | `64de0c7` | E10 (addition) | Added `experiments/e11_proxy_k.py` and a `proxy_k` argument to `fit_weights`/`ZSH` (the configured value 10 remains the default, so no earlier result changes): the pipeline is refitted with proxy partitions of 5, 20 and 50 clusters on the same 1,000,000-row sample as E10 and evaluated on the test period with the same protocol. | Reviewer 2 asked for sensitivity to the size of the proxy partition; the first version of the revision listed this as a limitation instead. | New result (Table 15 and Section 6.6): the feature ranking depends on the proxy size (Kendall τ 0.36–0.88) and concentration moves in both directions by large amounts. Nothing computed earlier changes. |
| 2026-09-18 | `cd0c3ae` | E4 (addition) | Added `experiments/e12_profile_support.py` (Table A3): the share of every profile in each period with a block-bootstrap interval, and the 5th to 95th percentile of its cluster-wise Jaccard similarity across the E4 refits. | Reviewer 2 asked for profile-support intervals alongside assignment and centroid stability. | New descriptive output; reads saved labels and E4 results. Nothing computed earlier changes. |
| 2026-09-18 | `cd0c3ae` | §2.3, §7.4 | Added a reference to a recent comparison of unsupervised detectors with a supervised graph model on the Elliptic data (Pérez-Cano and Jurado, 2025), checked against CrossRef and the article text. | Reviewer 1 asked that recent comparisons of unsupervised Bitcoin anomaly detection be considered. | Discussion only. |
| 2026-09-18 | `99a0287` | §3.6, §5.3, E8 (addition) | Added `experiments/e13_actor_holdout.py`: the actor annotations of Elliptic++ (`AddrTx_edgelist.csv`, `TxAddr_edgelist.csv` from github.com/git-disl/EllipticPlusPlus, checksums in `results/E13/summary.json`) are used to test whether the Elliptic evaluation can be made disjoint in actors, and to repeat it with the cluster ranking restricted to training transactions that share no address with the evaluation period. The data are used as labels only and are not redistributed here. | Reviewers 1 and 2 asked for evaluation on source-level held-out transactions. | New result (Section 6.5): a fully actor-disjoint split is impossible (one connected component; a random address split keeps 20.3% of labelled transactions), and restricting the ranking leaves the conclusion unchanged (AP lift 1.59 against 1.57). Nothing computed earlier changes. |
| 2026-09-18 | `99a0287` | §3.6, E7 (addition) | Added `experiments/e14_coinjoin_source.py`: a public list of Wasabi 2.x CoinJoin transactions with a known coordinator (from the artefact of Svenda et al., 2026; checksum in `results/E14/summary.json`) is used as a source-level label for the count rule and the equal-output rule. Used as a label only, not redistributed. | Reviewer 1 asked for validation of the heuristic families against something other than our own rules. | New result (Section 6.4, Table 12): recall 98.9% and 100% for the count rule, 97.8% for the equal-output rule; the labelled CoinJoins concentrate in one profile at about 200 times the base rate. |
| 2026-09-18 | `d282cde` | §5.3, §6.4, §6.5 | Concentration is now also reported as the share of its ceiling 1/π, which equals the average precision. No quantity is recomputed; the tables gain two columns and the text is rewritten around them. | The AP lift alone cannot distinguish an annotation that is nearly perfectly concentrated from one that is barely concentrated, because the ceiling depends on the base rate. | Presentation only. |
| 2026-09-18 | `d282cde` | E3 (addition) | Added `experiments/e15_oracle_weights.py` and a `fixed_w` argument to `ZSH` (default unchanged): the pipeline is refitted with weights computed from an annotation itself, as an upper bound for what any weighting of these features could reach. | To separate two explanations of weak concentration: a poor weighting or a feature set that does not carry the information. | New result (Section 6.6, Table 18): oracle weights give little or no gain and are worse for P2PKH, so the limit is not the weighting. Nothing computed earlier changes. |
| 2026-09-18 | `d282cde` | E5 (addition) | Added `experiments/e16_profile_matching.py`: each profile is matched to one cluster of the refit of the same period by minimum-cost assignment, scored by Jaccard similarity. The K-means++ refits, which E5 did not save, are refitted with the E5 seeds. | Refitting is the natural answer to drift, but it is only useful if the profiles can be followed. | New result (Section 6.3, Table 10): no profile reaches a Jaccard similarity of 0.5 after a refit. Nothing computed earlier changes. |
| 2026-09-19 | `b638192` | E2, E4, E5 (addition) | Added `experiments/e17_benchmark.py`: the whole evaluation (concentration with its ceiling on both later periods, ten block-bootstrap refits, a refit on each later period and the profile matching of E16) applied to the six comparison methods of Table 6 at K = 31, fitted on the same development transactions; BIRCH and the partial reproduction of Vlahavas et al. use a 1,000,000-row subsample because their cost grows faster than linearly, and HDBSCAN is excluded because it cannot assign new transactions. Presentation: two tables, one figure (`T_bench_battery`, `T_bench_attained`, `F13_bench`). | The confirmatory design compares ZSH with K-means++ only, so the limits reported in Sections 6.3 to 6.6 could not be attributed either to ZSH or to the task. | New result (Section 6.7): the ceiling on rare annotations, the collapse of the partition in the prospective period and the failure of profile matching hold for every family; the highest median attained share (22.6%) and the concentration of P2PKH inputs (80.9%) belong to ZSH. Nothing computed earlier changes; ZSH's own values reproduce those of E6 to one decimal place. |
| 2026-09-19 | `471c614` | §6 (presentation) | Twelve tables and six figures, the full definitions of the secondary measures and the former Appendix A are published as Supplementary Materials instead of inside the article (`v2_work/assemble.py` builds `supplementary.md` and its .docx; items are numbered S1, S2, ... in order of first mention). `verify_manuscript_numbers.py` now checks the article and the supplementary file together. | Reviewer 3 asked for a shorter article; the submitted version was 31 pages and the revision had grown to 48. | Presentation only: no result is deleted or recomputed. The article is 36 pages and all 275 checks still pass. |
