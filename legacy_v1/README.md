# Legacy v1 code

Scripts cited by the first manuscript submission (tag `v1-submitted`), moved
here unchanged. They are kept for traceability only. Known problems that the
v2 study corrects are described in `../ANALYSIS_PLAN.md`; none of the v1
results are reused.
